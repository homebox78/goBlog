# goBlog 다음 작업 체크리스트

> 최종 갱신: 2026-07-14 · A(성과 수집)·B(자가학습 골격)·C(내부 링크) 구현 완료 — 성과 표본이 모이면 학습이 자동 시작
> 목표: 기획→작성→검수→광고→발행→**성과분석→개선**까지 도는 **자가발전형 AI 블로거**

---

## 지금 상태 (코드·DB로 검증한 사실)

| 항목 | 상태 | 근거 |
|---|---|---|
| 인용 글 수집 (네이버 AI 브리핑 인용 배지) | ✅ | 매일 07:30 크론 · `blog_citations` |
| 인용 글 **학습** ("왜 인용됐나" 분석) | ✅ | `citation_insights` 10건 축적 |
| 학습 → **글 생성 프롬프트 주입** | ✅ | 문체 실측치가 실제로 프롬프트에 박힘 |
| 실시간 그라운딩 (생성 시점 최신 뉴스 주입) | ✅ | `articles/grounding.ts` |
| 검색 의도 5분류 → 글 유형 자동 결정 | ✅ | `keywords/engine.ts` |
| 중복 글 방지 (유사하면 생성 409 차단) | ✅ | `articles/similarity.ts` |
| 근접 중복 **키워드** 차단 | ✅ | `keywords/near-duplicate.ts` (2026-07-14 추가) |
| 이미지 유실 방지 (DB 가드·무결성 점검·덮어쓰기 차단) | ✅ | 2026-07-14 추가 |
| **성과 데이터 수집** | ✅ **구현 완료** (2026-07-14) | GSC 수집기 + 매일 05:00 크론 + 대시보드 표시. 우리 3개 속성은 **오늘 소유확인** → GSC는 소급 안 하므로 2~4일 뒤부터 데이터 유입 |
| **성과 → 다음 글 반영 (자가발전 루프)** | 🔶 **골격 완성, 학습 대기** | `self-learning.ts` — 관측·비교·규칙 도출·프롬프트 주입까지 구현. **성과 신호 붙은 글 0/6건**이라 학습은 보류 중(정직) |
| 내부 링크 자동 연결 | ✅ **구현 완료** (2026-07-14) | 생성 시 `goblog://article/<id>` → 발행 시 **플랫폼별 자기 글 URL**로 치환 |
| 오래된 글 자동 갱신 | 🔶 부분 | 품질점수 보정만. 순위 하락·경과일 트리거 없음 |

### 핵심 진단
**"남의 글(경쟁 블로그) 학습 루프"는 돌고 있지만, "내 글 성과 루프"는 존재하지 않는다.**
발행하면 거기서 끝. 노출수·클릭률·체류시간·수익이 하나도 안 들어온다.
지금 대시보드 숫자(글 수·발행 성공·품질점수·AI 비용)는 **내부 생산 지표**지 독자 반응이 아니다.

---

## A. 성과 데이터 수집 — **최우선** (이게 없으면 B·C는 의미 없음)

- [x] **A-0. 선행 확인** — 완료. ⚠️ GSC에 **등록된 사이트가 하나도 없었다**(로드맵의 "Blogger 자동 연결"은 사실이 아니었음): Search Console에 각 발행처가 **사이트 등록 + 소유확인** 되어 있는지
  - [x] 워드프레스 — 확인 파일 업로드(`/var/www/html/wordpress/google*.html`)로 인증
  - [x] 티스토리 — 스킨 `<head>`에 메타태그 삽입해 인증
  - [x] Blogger — 속성 추가하니 자동 확인됨
  - [ ] 네이버 블로그 → **GSC 대상 아님**(미착수). 네이버 서치어드바이저 / 네이버 애널리틱스 별도 필요
- [x] **A-1. OAuth 스코프 확장** — 앱 안에 OAuth 흐름 구현(`settings/google-oauth.router.ts`). 동의 완료, 토큰에 `blogger`+`webmasters.readonly` 포함 — Blogger에 쓰는 구글 계정 그대로 재사용 (신규 키 발급 불필요, 무료)
  - `webmasters.readonly` (Search Console)
  - AdSense는 별도 스코프 + 계정 승인 상태 확인
- [x] **A-2. 수집기 구현** `apps/api/src/modules/analytics/collectors/`
  - GSC: 일자별 노출수·클릭수·CTR·평균순위·**유입 쿼리** (page 단위)
  - AdSense: 일자별 수익·RPM (page 단위)
  - `publish_jobs.publishedUrl` ↔ GSC page URL 매칭으로 **글 단위 성과** 연결
- [x] **A-3. 저장** — `analytics_daily` upsert (재수집해도 중복 없음) — 기존 `AnalyticsDaily` 모델 사용 (스키마 이미 존재)
  - 부족한 필드 확인: 체류시간·이탈률은 GSC에 없음 → GA4 필요 여부 판단
- [x] **A-4. 스케줄** — 매일 05:00 KST, 최근 5일 재수집 — 하루 1회 (GSC 데이터는 2~3일 지연됨을 감안)
- [x] **A-5. 대시보드** — 글별 노출·클릭·평균순위 랭킹. 데이터 없으면 0이 아니라 "수집 전"으로 표시 — 글별 노출/클릭/순위/수익 표시 + 상위·하위 글 랭킹

**비용: 무료** (GSC/AdSense API 무료). 리스크: GSC 데이터 지연·샘플링, 네이버는 별도 경로.

## B. 성과 → 생성 반영 (자가발전 루프 닫기)
> 골격은 완성됐다. **표본이 모이면 스스로 켜진다** (성과 신호 6건부터 학습 시작).

- [x] B-1. 성과 상위/하위 글 비교 → 규칙 도출 → 생성 프롬프트 주입 (`self-learning.ts`)
  - 관측 지표: GSC 노출·클릭·평균순위 + **네이버 AI 인용 여부** + 제목 길이·본문 길이·H2 수·표·FAQ·발행 시각
  - ⚠️ **표본 6건 미만이면 학습하지 않는다** — 없는 데이터로 규칙을 지어내면 '배운 척'하는 시스템이 된다
  - ⚠️ 발행 직후 글(daysLive 작음)을 '실패'로 오독하지 않도록 프롬프트에 명시
  - 대시보드에 "관측 중 (0/6건)" 정직하게 표시
- [ ] B-2. 성과 하위 글 원인 진단 → 개선안 자동 제안 (승인 후 재작성)
- [ ] B-3. GSC **유입 쿼리**를 키워드 발굴에 역주입 (실제로 들어온 검색어 = 가장 확실한 신호)
- [ ] B-4. ⚠️ 단기 CTR만 좇아 자극적 제목으로 흐르지 않도록 가드 (사용자 방침)
- [ ] B-5. **키워드 트렌드(190행 축적)를 아직 학습에 안 쓴다** — 기록만 하고 선정·생성에 반영 안 됨

## C. 내부 링크 자동 연결
- [x] C-1. 새 글 작성 시 관련 기존 글(`publishedUrl`)을 본문에 자연스럽게 연결
- [x] C-2. 플랫폼별로 자기 글 URL 사용 — 대상 글이 그 플랫폼에 없으면 **링크를 벗기고 글자만 남긴다**(그럴듯한 다른 URL로 대신 걸지 않는다) (네이버 글엔 네이버 URL)

---

## 지식 계층 (2026-07-14 구축 완료)

| 계층 | 무엇을 배우나 | 저장 | 주입 시점 |
|---|---|---|---|
| **전역 지식** | 인용 분석 13개 주제를 종합한 **주제 무관 법칙** | `__KNOWLEDGE__:GLOBAL` | 모든 글 (항상) |
| **키워드 인사이트** | 그 키워드에서 인용된 글의 패턴·빈 각도 | `citation_insights` | 해당 키워드 글 |
| **문체 프로파일** | 인용 상위 블로거의 실측 문체 | `__STYLE__:<플랫폼>` | 모든 글 (플랫폼별) |
| **자가학습** | **내 글의 실제 성과**에서 배운 규칙 | `__SELF__:PERFORMANCE` | 모든 글 (표본 모이면) |

⚠️ 예전엔 학습이 **그 키워드에만 갇혀 있었다** — 새 키워드로 쓰면 매번 백지에서 시작했다.
전역 지식 계층이 그걸 뚫었다. **평문 지식이라 AI 엔진이 바뀌어도 그대로 쓰인다.**

## 남은 수동 작업 (2026-07-14 시점)

- [ ] **Search Console API 사용 설정** — 완료(사용자가 켬). 403 원인이었음
- [ ] 구글 클라우드 콘솔 리디렉션 URI 등록 — 완료
- [ ] ⏳ **2~4일 뒤 성과 데이터 유입 확인** (`POST /api/analytics/collect` 로 즉시 확인 가능)
- [ ] 티스토리 `<title>` 중복(`[##_page_title_##] :: [##_title_##]`) — 스킨 편집으로 수정 가능

## 별도 확인 필요 (미해결)

- [ ] 네이버 발행 글 1건 제목이 `"🔥"` 하나로 깨짐 → 네이버에서 직접 수정 필요
  - https://blog.naver.com/coreselect/224345223842
  - 올바른 제목: `SK하이닉스 레버리지 ETF와 ADR, 하락의 신호탄이었을까`
  - (원인은 수정 완료 — 확장 v0.1.29에서 제목 검증·재시도·발행차단)
- [ ] 워드프레스 폰트 설치 재시도 (권한 문제는 해결됨, 사용자 확인 전)
- [ ] 확장 프로그램 `chrome://extensions`에서 새로고침 (v0.1.29)

---

## 개발 시 주의 (실제로 사고 났던 것들)

- **이미지 생성은 배포된 서버에서만** — 로컬 개발 API로 만들면 `MEDIA_PUBLIC_URL`이 없어 죽은 링크가
  공유 운영 DB에 박힌다. (지금은 DB 가드가 막지만, 애초에 서버에서 하는 게 맞다)
- **이미지 재생성 후에는 새로고침** 하고 편집할 것. 오래된 화면에서 저장하면 본문이 되돌아간다
  (지금은 409로 막힘).
- **배포는 작업 중이 아닐 때** — 우아한 종료를 넣어 진행 중 요청은 살아남지만, 굳이 겹칠 필요 없다.

---

## 개발 중 얻은 교훈 (2026-07-14 추가)

- **403은 원인이 두 갈래다** — 'API 미사용 설정' vs '권한 없음'. 상태코드만 로그에 남기면 엉뚱한 곳을 뒤진다.
  오류에 **응답 본문을 함께 실어라**. (GSC 403 = Search Console API가 꺼져 있던 것)
- **GSC는 소유확인 이전 데이터를 소급해주지 않는다.** 오늘 확인했으면 오늘부터 쌓인다(+2~3일 지연).
- **매칭 0건일 때 "성과 없음"과 "URL 안 맞음"은 다르다** — 미매칭 URL 샘플을 반환하게 해서 즉시 구분되게 함.
- **내부 링크는 플랫폼별로 달라야 한다.** 네이버 글에서 티스토리로 보내면 독자가 남의 집으로 샌다.
  대상 글이 그 플랫폼에 없으면 **링크를 지운다** — "그럴듯한 다른 URL"로 대신 걸면 조용한 오연결이다.
